#!/bin/bash

#Script that checks if a directory named backup exists in the current directory. If it does not exist, create it.

if

[ ! -d "$dldir" ] && mkdir -p "$dldir"

test -d /path/to/directory; then
  echo "Directory exists."
fi
